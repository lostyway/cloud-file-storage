<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
        xmlns:pro="http://www.liquibase.org/xml/ns/pro"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
        http://www.liquibase.org/xml/ns/dbchangelog-ext
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd
        http://www.liquibase.org/xml/ns/pro
        http://www.liquibase.org/xml/ns/pro/liquibase-pro-latest.xsd">
    <changeSet id="1" author="lostway">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="uploaded_files"/>
            </not>
        </preConditions>
        <comment>Создание таблицы загруженного файла</comment>
        <sql>
            CREATE TABLE uploaded_files
            (
                file_id        UUID PRIMARY KEY,
                file_name      VARCHAR(255) NOT NULL,
                full_path      VARCHAR(255) NOT NULL,
                content_type   VARCHAR(100),
                file_size      BIGINT,
                uploader_email VARCHAR(255),
                status         VARCHAR(50)  NOT NULL,
                notes          TEXT,
                created_at     TIMESTAMP    NOT NULL DEFAULT NOW(),
                updated_at     TIMESTAMP    NOT NULL DEFAULT NOW()
            );
        </sql>
    </changeSet>

    <changeSet id="2" author="lostway">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="outbox_kafka"/>
            </not>
        </preConditions>
        <comment>Создание таблицы кафка события</comment>
        <sql>
            CREATE TABLE outbox_kafka
            (
                id             BIGSERIAL PRIMARY KEY,
                file_id   UUID,
                payload        JSONB,
                created_at     TIMESTAMP NOT NULL DEFAULT now(),
                processed      BOOLEAN   NOT NULL DEFAULT false
            );
        </sql>
    </changeSet>
</databaseChangeLog>